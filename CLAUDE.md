# AI reminders

- Don't self-verify UI changes by driving the browser after implementing a fix or feature. Compile/type-check as usual, start or refresh the dev server, and report what changed — the user will check the UI themselves.
